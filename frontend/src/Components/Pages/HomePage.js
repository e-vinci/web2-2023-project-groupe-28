const HomePage = () => {
  const main = document.querySelector('main');
  main.innerHTML = `
    <div id="neon">
      <span>Battle</span>
      <span id="ship-neon">Ship</span>
    </div>

    
  `;


};

export default HomePage;
